function filter(){
    var price = document.getElementById("price").value;
    var items = document.getElementsByClassName("card").value;
    alert(items);
    // if (price == "below10" && items == "RM 50") {
    //     items.style.display = 'none';
    // }
    // items.style.display = 'none';
}
